framework:
    test: true
    session:
        storage_factory_id: session.storage.factory.mock_file
    profiler:
        collect: false
    router:
        resource: '.'
        type: annotation
    templating:
        engines: ['twig']
    validation:
        enabled: true
    mailer:
        enabled: true
        dsn: 'null://null'
    messenger:
        enabled: true
        transports:
            async: 'null://'
    csrf_protection:
        enabled: true 
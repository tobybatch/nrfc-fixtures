<nav class="bg-secondary py-2">
    <div class="container mx-auto px-4">
        <ul class="flex justify-between items-center">
            <!-- Left group -->
            <div class="flex">
                <li class="mr-4">
                    <a href="{{ path('app_fixture_index') }}"
                       class="text-white px-4 py-2 rounded hover:bg-tertiary transition">
                        Fixtures
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_club_index') }}"
                       class="text-white px-4 py-2 rounded hover:bg-tertiary transition">
                        Clubs
                    </a>
                </li>
            </div>
        </ul>
    </div>
</nav>